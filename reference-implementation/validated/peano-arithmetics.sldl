Sentence {
    Suc(n) -> "The succesor of" + n,
    Add(x, y, z) -> {
        sum: "The sum of " + x + " and " + y + z,
        solved: "The solution is " + (x | y),
    },
    Eq(x, y) -> x + " is equals to " + y,
}
